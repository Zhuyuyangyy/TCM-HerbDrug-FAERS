# TCM-HerbDrug-FAERS — Q2 2026 Review

**Scope reviewed:** `backend/main.py`, `models/{disproportionality,signal_detector,mechanism_graph,temporal_signal}.py`, `analysis/{faers_pipeline,risk_scoring,evidence_chain,validation}.py`, `data/{herb_kg,cyp_mapper}.py`, API routers.

**7-Dim Rubric (100 pts total)**

| Dim | Score | Notes |
|---|---|---|
| 1. Scientific soundness | 14/15 | Five metrics (ROR/PRR/IC/BCPNN/MGPS) with Haldane-Anscombe correction and Bayesian posterior on alpha=0.5. P-value derived from ROR CI via A&S 26.2.17; BH-FDR at alpha=0.05. Only deduction: 95% Wald CI on log scale, fine for moderate n. |
| 2. Domain coverage | 14/15 | 16-herb KG (Danshen, St John's Wort, Red Yeast Rice, Saw Palmetto…) with inhibition+induction+P-gp. Covers warfarin, cyclosporine, statin, digoxin, OCP major HDIs. Mechanism graph encodes 13+ relation types with templated explanations. |
| 3. Algorithmic depth | 14/15 | 2x2 disproportionality, MGPS shrinkage (alpha=0.2 prior), log-normal EB05/EB95, temporal log-linear regression with t-test p, bootstrap CI for risk (1000 resamples, fixed seed), evidence chain with geometric-mean confidence. Real sophistication. |
| 4. Engineering quality | 13/15 | Clean dataclasses, FastAPI routers prefixed, /health + /api/summary endpoints, pydantic-free (hand-rolled dataclasses). Tests under `backend/tests/` (test_disproportionality, test_signal, test_api). Scripts include `quick_smoke_test`, `run_synthetic_validation`. Could use pydantic for request models. |
| 5. Reproducibility | 13/15 | `REPRODUCE.md` present, Dockerfile + `pyproject.toml`. Fixed random seed (42) in bootstrap and synthetic generator. Deterministic Weibull MLE fallback to method-of-moments. |
| 6. Validation rigor | 14/15 | 7 known positive controls (warfarin/danshen, digoxin/gancao, cyclosporine/StJohnWort) cross-referenced; sensitivity reported, specificity deferred (acknowledged). Bootstrap CI on risk score, FDR-corrected signals. |
| 7. Communication | 12/15 | Module docstrings reference Evans 2001, Bate 1998, Norén 2006 (BCPNN). Bilingual 中/英 in places. `/api/summary` is informative. Manuscript-ready text report generator in `faers_pipeline.py`. Could add inline theorem citations. |

**Total: 94/100** — Class A, competition-ready. Strongest in the four for SCI reproducibility.

**Key strengths**
- MGPS + classical disproportionality + BCPNN triple coverage is rare for a TCM platform.
- Evidence-chain builder unifies signal + DB + mechanism with explicit confidence propagation.
- Temporal module adds real novelty over typical PV pipelines.

**Key weaknesses**
- `herb_name_normalizer` integration in `faers_pipeline` still uses regex patterns; no UMLS/CUI mapping.
- Specificity set to 1.0 by default (`_run_validation` line 360) — honest but uninformative.
- NetworkX-based path search caps at cutoff=5; may miss longer mechanism chains.
